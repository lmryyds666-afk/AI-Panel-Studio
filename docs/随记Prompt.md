## Prompt0 项目初始化提示词

```
# 顶层全局开发基准约束（所有子任务强制遵循）
项目名称：AI Panel Studio AI圆桌讨论演播厅
技术栈：
后端：Node.js + Express + Prisma ORM + Socket.io + Jest单元测试
前端：Vite + React + TypeScript + TailwindCSS + Cypress E2E自动化测试
大模型依赖：Deepseek V4 Pro API

全局硬性通用规则，所有开发子任务必须遵守：
1. 任务隔离原则：单次指令仅开发单一独立模块，禁止跨模块混合生成代码；仅输出当前任务指定文件，无关业务代码完全屏蔽；
2. 分层开发顺序锁死：SDD(数据层) → DDD(前端页面层) → TDD(后端接口/服务/单元测试) → E2E自动化测试 → 业务AI服务 → 前端视觉优化 → 调试辅助页面 → 交付文档；底层未完成不允许上层超前开发；
3. 数据隔离强制规范：全业务逻辑以discussion_id作为唯一隔离标识，多场讨论上下文、WebSocket房间、数据库数据完全隔离，杜绝串流、数据混淆；
4. AI调用统一约束：所有大模型请求强制指定固定JSON输出Schema，添加字段校验、异常重试、限流逻辑，抑制自由文本幻觉，保证前端可稳定解析返回数据；
5. 测试兜底规范：所有后端服务/接口配套Jest单元测试；全前端页面配套Cypress E2E测试用例；新增功能必须同步补充对应测试；
6. 视觉统一规范：前端全局使用长白山天池自然风光半透明磨砂背景，所有卡片/弹窗使用半透深色玻璃遮罩；全页面底部固定展示页脚「AI Panel Studio | 李梦冉 | 13833818010」；演播类页面右上角自带返回首页按钮；
7. 交付归档规范：每完成一个模块，需留存本条任务完整提示词+开发意图，统一归档至docs/Prompt记录.md，覆盖SDD/DDD/TDD/E2E四大阶段，记录总段落≥5段；
8. 交付文档硬性产出：
   ① ≥5套预设讨论JSON/SQL种子样例数据
   ② 1~1.5页AI协同开发工作流说明文档（包含典型问题+解决方案）
   ③ 完整项目README部署文档
   ④ 全流程Prompt归档记录文档

后续下发所有细分模块任务，均基于本套顶层基准约束执行，细分任务仅补充当前模块专属业务需求，不覆盖、不违反本全局规则。

```



## Prompt 1 SQLite数据建模（ER+Prisma Schema)

/superpowers:spec-driven-development 

```
【SDD阶段｜单一任务：仅完成SQLite数据建模】 
基于AI Panel Studio AI圆桌Web App需求，仅输出两部分内容： 
1. Mermaid完整ER图代码；
2. Prisma Schema模型代码。 
约束规则： 
1. 实体包含Discussion、Guest、Speech、ConsensusRecord，所有数据通过discussion_id隔离多场讨论； 2. Guest字段包含姓名、职业、标题、立场、专属颜色、运行状态（待机/准备发言/发言中）； 
3. ConsensusRecord区分共识、分歧两种类型；
4. 禁止输出接口代码、前端代码、业务逻辑，只输出建模相关内容。
```

```
记录说明
意图：先锁死底层数据结构，避免后续开发字段缺失、多讨论串数据；
挑战：容易遗漏嘉宾状态、共识分歧区分字段，通过强约束限制输出范围，只输出模型，不溢出代码。
```



## Prompt2 API+Websocket规范文档

/superpowers:spec-driven-development

```
【SDD阶段｜单一任务：仅输出API与实时事件文档】 
基于上一轮生成的Prisma数据模型，只输出接口规范文档，禁止写任何接口实现代码。
要求：
1. 输出RESTful接口清单：创建讨论、获取讨论列表、生成嘉宾、结束讨论； 
2. 输出WebSocket实时事件定义：嘉宾状态更新、新发言、共识分歧刷新、总结推送；
3. 所有接口/事件必须携带discussion_id，保证多讨论数据隔离；
4. 只输出markdown格式文档，不生成前端、后端业务代码。
```

```
记录说明
意图：统一前后端交互标准，后续开发不用反复对齐字段；
挑战：AI 容易顺带写接口业务代码，增加约束只输出文档，规避一键生成违规
```





## Prompt 3 前端页面布局+组件树设计

```
【DDD阶段｜任务：前端页面与组件拆分设计】
需求：AI圆桌演播厅中文响应式页面，分三大页面：首页讨论列表、嘉宾配置页、演播厅主页面。
演播厅硬性布局要求：
1. 顶部：讨论标题+结束按钮；
2. 左侧独立滚动区域：多嘉宾状态小卡片，显示角色、状态、专属颜色、思考摘要；
3. 中间独立滚动：发言记录Transcript，色块区分发言人；
4. 右侧独立滚动：实时共识/分歧面板；
5. 全页面分区域独立滚动，适配超宽屏/桌面/移动端窄屏。
输出：Mermaid页面布局示意图、全部组件拆分清单、每个组件的入参与交互逻辑；
禁止直接输出完整可运行前端页面代码，仅输出设计文档。
```

```
记录说明
意图：以 UI 视觉驱动开发，保证演播厅沉浸感，提前拆分组件避免页面混乱；
挑战：AI 会直接生成完整 React 页面代码，增加约束仅输出设计方案，分阶段再实现组件。
```



## Prompt4 嘉宾生成服务+Jest单元测试

```
【TDD阶段｜单一任务：嘉宾生成逻辑+Jest单元测试】
依赖SDD阶段Prisma模型，接入Deepseek V4 Pro。
要求：
1. 先编写完整单元测试用例，覆盖：默认4人、嘉宾立场/颜色不重复、多讨论隔离；
2. 再实现单独的嘉宾生成service函数，增加JSON结构校验，抑制大模型幻觉；
3. 只做嘉宾生成功能，不包含演播厅发言、实时推送逻辑；
输出：测试文件完整代码、guest生成服务代码。
```

```
记录说明
意图：遵循测试驱动，先断言后实现，保证大模型返回数据稳定；
挑战：大模型返回格式错乱，通过测试用例强制校验字段，减少幻觉问题。
```



## Prompt5 Cypress 全流程测试脚本

```
【E2E阶段｜单一任务：全流程端到端测试脚本】
覆盖完整用户操作链路：新建讨论→生成嘉宾→演播厅实时发言查看→查看共识分歧→结束讨论查看总结。
要求模拟WebSocket数据隔离，不同discussion_id数据互不干扰；
仅输出Cypress E2E测试代码，附带执行步骤说明，不修改前后端业务代码
```

```
记录说明
意图：验证整套系统端到端可用性，检查多讨论隔离、实时推送是否失效；
挑战：E2E 容易包含前后端全套业务改造，限定仅输出测试脚本，不改动业务代码。
```

=======================================================================================



## Prompt6 后端API路由

```
【TDD阶段｜单一任务：后端REST API路由、处理器、通用中间件完整实现】
前置依赖：SDD阶段API规范md文档、Prisma数据模型、已完成嘉宾生成service代码。
硬性约束：
1. 仅输出后端API相关代码，禁止生成WebSocket、前端页面、AI发言调度、共识提炼相关任何代码；
2. 完成三部分内容：
   ① 通用中间件：全局参数校验中间件、统一错误处理中间件、请求日志中间件；
   ② 6个REST端点完整路由层routes代码；
   ③ 对应路由处理器controller函数，内部完成数据库查询、调用已有业务service、标准化返回响应；
3. 所有接口强制校验discussion_id，实现多讨论数据隔离，使用Zod做入参校验；
4. 遵循分层结构 routes → controller → service，配套新增接口层Jest单元测试；
输出内容：完整中间件文件、路由文件、控制器文件、新增测试代码，标注每个文件在backend目录下的存放路径。
```

```
开发意图：基于已定义的 API 规范落地可运行接口底座，配套全局校验与错误中间件，保证接口统一规范；
遇到挑战：AI 容易顺带生成 WebSocket / 前端代码，通过强约束限制输出范围，只保留后端接口相关内容；
处理方式：过滤无关输出，手动将代码分层存入对应 backend 文件夹，单独 Git 提交
```



## Prompt7 前端应用

```
【DDD阶段｜单一任务：前端React演播厅完整页面实现】
前置依赖：DDD页面布局组件树设计文档、已完成后端6个REST API接口规范、Cypress E2E测试脚本。
硬性约束：
1. 仅输出前端React+Tailwind CSS代码，禁止生成后端、WebSocket、AI发言调度、共识提炼相关任何代码；
2. 完整实现三大页面：首页讨论列表页、嘉宾配置页、演播厅三栏主页面；
3. 演播厅强制三栏独立滚动布局：左侧嘉宾卡片区、中间发言记录面板、右侧共识分歧面板，完成全响应式适配（超宽屏/桌面/窄屏）；
4. 封装通用API请求hooks，预留WebSocket订阅hooks（仅定义空封装，不实现后端WS逻辑），每个组件遵循单一职责；
5. 嘉宾卡片展示专属区分色、待机/发言中状态标识；
输出内容：所有页面、组件、hooks完整代码，标注前端目录存放结构。
```

```
开发意图：基于已完成的后端 REST 接口与前端布局设计文档，落地全套可交互演播厅页面；封装接口请求能力，搭建三栏响应式 UI 框架，预留 WebSocket 订阅入口，为后续实时通信、AI 圆桌业务提供前端交互载体，同时支撑 E2E 自动化测试校验。
遇到的挑战：前端页面搭建完成，启动于localhost:5173,报错：500，原因：后端启动报错，导致前后端联调失败。
```



## Prompt8种子样例数据专用提示词

```
【收尾任务｜单一模块：Prisma种子样例数据生成】
前置依赖：完整Prisma Schema数据模型。
硬性约束：
1. 仅输出prisma/seed.ts完整种子脚本，禁止生成后端接口、前端、AI调度相关代码；
2. 包含至少5组完整预设讨论，每组自带独立话题、主持人、多位专家嘉宾阵容；
3. 数据满足多discussion_id隔离，字段完全匹配模型定义；
4. 附带README内数据使用说明：执行npx prisma db seed即可初始化；
输出：完整seed.ts脚本、样例数据使用说明文本。
```

```
开发意图：生成 5 套独立完整的预设讨论样例数据，实现项目初始化一键填充演示内容，交付后开箱即可演示完整圆桌流程，满足交付硬性要求。
```



## Prompt 9 WebSocket 实时通信服务

```
【TDD阶段｜单一任务：后端Socket.io WebSocket实时服务开发】
前置依赖：
1. SDD阶段WebSocket事件规范文档
2. 已完成6个REST后端接口、Prisma完整数据模型
3. 已生成5套prisma种子样例数据

硬性约束：
1. 仅生成后端WebSocket服务代码，禁止输出前端页面、AI发言调度、共识提炼相关任何代码；
2. 基于discussion_id实现Socket房间隔离，不同讨论频道数据完全隔离，不会互相广播；
3. 完整实现约定4类实时推送事件：
   - guest_status_update：嘉宾状态变更（待机/发言中）推送
   - new_speech：新发言记录入库后实时广播
   - consensus_refresh：共识、分歧内容更新推送
   - panel_summary：整场讨论总结推送
4. 配套Jest单元测试，验证多房间隔离、事件广播、客户端上下线逻辑；
5. 统一跨域配置，与现有Express后端服务集成；
输出内容：
1. ws服务完整源码文件
2. 配套单元测试代码
3. 文件存放路径（backend/ws/）
4. 后端服务入口server.ts集成修改片段
```

```
开发意图：开发实时消息推送底层服务，通过 discussion_id 划分独立演播厅频道，实现嘉宾状态、发言、共识数据的实时前端推送；补齐项目实时交互核心底座，为后续 AI 自动圆桌发言、共识实时更新提供事件分发能力，同时配套单元测试保障多讨论数据隔离无串流 bug。
```





## Prompt 10 AI调度引擎

```
【TDD阶段｜单一任务：AI圆桌发言调度服务开发】
前置依赖：
1. 已完成后端REST API、WebSocket实时广播服务、Prisma数据模型
2. 5套完整种子样例数据、SDD AI交互规范文档
硬性约束：
1. 仅生成后端调度service代码与单元测试，禁止输出前端、WebSocket基础服务、共识提炼代码；
2. 核心调度规则强制实现：
   ① 拒绝机械轮流发言，支持主持人控场、专家主动抢答、补充、反驳三种发言行为；
   ② 单次AI输出发言控制为1-2短句，避免大段文本；
   ③ 按discussion_id隔离每场讨论对话上下文，不同讨论记忆完全独立；
   ④ 自动维护每位嘉宾运行状态：待机/发言中，状态变更触发WebSocket广播；
3. 对接Deepseek V4 Pro大模型API，封装统一请求层，增加JSON输出格式约束抑制幻觉；
4. 发言生成完成后自动写入Speech数据表，触发new_speech实时推送事件；
5. 配套完整Jest单元测试，覆盖上下文隔离、多发言行为、状态切换场景；
输出内容：
1. 调度服务完整源码（backend/services/speechScheduler.ts）
2. 对应单元测试文件
3. 对外调用接口说明、依赖注入集成片段
```

```
开发意图：本模块为项目核心业务逻辑，实现主持人 + 多专家拟人化圆桌互动调度；通过差异化发言行为打破固定轮流模式，隔离单场讨论对话上下文，控制 AI 输出篇幅，发言落地入库并推送实时消息，赋予演播厅自动讨论交互能力，配套单元测试规避多场讨论上下文串扰、嘉宾状态错乱问题。
遇到的挑战：
1.后端研讨详情接口仅返回发言数量，未携带完整发言数组；
2.前端初始化逻辑无法从接口拿到历史发言数据；
3.WebSocket 仅为空白占位逻辑，无法接收实时新增发言推送；
双重数据源缺失导致发言面板一直显示 “等待发言...”（演播厅发言记录不显示）。
4.开发模式下会导致 WebSocket 连接后立即断开（双挂载效应），刚已修复（移除了 StrictMode）
```



## Prompt 11 共识提炼服务

```
【TDD阶段｜单一任务：实时共识分歧提炼服务开发】
前置依赖：
1. 已完成REST API、WebSocket服务、AI发言调度引擎、完整Prisma模型
2. SDD AI共识提取接口规范文档、种子演示数据
硬性约束：
1. 仅输出后端共识提炼service代码与单元测试，禁止生成前端、调度引擎、基础WS底层代码；
2. 业务逻辑规则：
   ① 监听new_speech事件，每条新发言异步调用Deepseek接口；
   ② 区分本场讨论「达成共识点」「存在分歧点」两类结果，结构化返回；
   ③ 所有数据绑定discussion_id，多场讨论数据完全隔离互不干扰；
   ④ 提炼结果存入Consensus数据表，触发consensus_refresh WS事件推送到前端右侧面板；
3. 增加接口限流、失败重试机制，防止高频发言造成API超限；
4. 强制大模型输出固定JSON结构，消除自由文本幻觉；
5. 配套Jest单元测试，覆盖多讨论隔离、发言增量提炼、异常重试场景；
输出内容：
1. 共识提炼完整服务代码 backend/services/consensusExtractor.ts
2. 配套单元测试文件
3. WS事件监听集成代码片段
```

```
开发意图：监听圆桌实时发言流，异步调用大模型增量提取每场讨论的共识与分歧观点，结构化持久化存储并实时推送前端展示；完善演播厅核心分析能力，和发言调度引擎配合完成完整 AI 圆桌闭环，配套测试保证多场讨论数据隔离、接口调用稳定。
```



## Prompt 12前端页面美化改造专用完整提示词

```
【DDD阶段｜单一任务：全局前端页面视觉美化改造】
前置依赖：现有完整React三页面代码、三栏演播厅页面、讨论列表首页代码
硬性约束：
1. 仅修改前端样式、新增底部页脚、右上角返回首页按钮，禁止改动后端接口、WebSocket、AI调度业务逻辑代码；
2. 背景需求：
   全局页面底层设置长白山天池自然风光图作为半透明磨砂背景，给所有卡片、面板、弹窗增加半透深色磨砂玻璃遮罩，保证文字清晰可读，不遮挡背景自然风景；
3. 全局页脚新增：所有页面底部固定展示页脚文本「AI Panel Studio | 李梦冉 | 13833818010」，居中浅色小字，不遮挡核心操作区域；
4. 右上角新增返回首页按钮：
   首页页面隐藏按钮，演播厅详情页面右上角固定悬浮「返回首页」圆角紫色按钮，点击路由跳转至讨论列表首页；
5. 去AI生硬工业深色单调风格优化：
   弱化纯黑死板底色，降低面板深色饱和度，柔和渐变阴影，调整按钮、标签色彩柔和度，去除机械冰冷的纯黑全屏风格，融合自然风光柔和氛围感，保留原有布局、所有功能交互、文字逻辑完全不变；
6. 保持全页面响应式适配，窄屏移动端页脚、按钮正常适配不溢出；
输出内容：
1. 全局样式修改代码（全局背景、磨砂玻璃卡片样式）
2. 页脚公共组件完整代码
3. 右上角返回首页按钮组件+路由绑定代码
4. 所有页面引入修改片段，标注文件存放路径
```

```
开发意图：优化原有纯黑单调工业风前端页面，引入自然风景半透背景弱化压抑深色感；新增统一底部个人信息页脚与演播厅返回首页快捷按钮，弱化生硬 AI 工具视觉感，在不改动原有业务功能、交互逻辑的前提下提升页面柔和自然观感，同时完善页面导航与版权信息。
```



## Prompt 13 生成完整标准化项目 README.md

```
【交付文档任务｜单一任务：项目完整README编写】
前置依赖：全项目前后端代码、AI调度/共识模块、WebSocket、美化后前端页面、种子样例数据全部开发完毕
硬性约束：
1. 仅输出Markdown文档，不生成任何业务代码；
2. 文档必须包含以下完整板块：
   1）项目简介：AI圆桌演播厅AI Panel Studio 功能概述，说明项目价值；
   2）开发技术栈：后端Express+Prisma+Socket.io，前端React+Vite+Tailwind；
   3）本地完整部署步骤：环境安装、数据库迁移、种子数据导入、后端启动、前端启动完整命令；
   4）核心功能模块分块介绍：REST底座、实时WebSocket、AI发言调度、共识提炼、美化前端页面；
   5）演示操作流程：新建讨论、进入演播厅、自动AI圆桌、实时共识查看完整操作；
   6）环境变量配置说明：数据库、大模型API密钥配置项；
   7）项目作者信息：AI Panel Studio | 李梦冉 | 13833818010；
3. 步骤适配SQLite本地开发，新手可一键复现完整演示流程；
输出：根目录README.md完整全文。
```

```
开发意图：编写项目一站式部署使用文档，梳理从环境搭建到完整功能演示全流程，标注项目作者信息，保证评审人员可快速本地运行项目，满足交付文档硬性要求。
```



## Prompt14 完整开发工作流文档

```
【交付文档任务｜单一任务：AI协同分层开发工作流说明文档】
前置依赖：AI Panel Studio 全项目开发完整流程、所有前后端/AI模块、前端美化、数据库可视化开发经历，真实踩坑问题记录
硬性约束：
1. 仅输出Markdown文档，不生成任何业务代码；
2. 篇幅严格控制在1～1.5页，结构清晰，逻辑连贯；
3. 文档必须包含三大核心板块：
## 一、项目标准分层开发完整流程（时间线顺序）
严格按照本次项目真实开发顺序梳理：
1. SDD阶段：Prisma数据库建模→ER图绘制→前后端API/WebSocket契约定义
2. DDD阶段：PRD需求梳理、页面结构设计、React组件树拆分、基础页面开发
3. TDD阶段：后端REST接口、通用中间件、单元测试编写；E2E自动化测试开发
4. 基础业务底座：种子样例数据、WebSocket实时房间广播服务
5. 核心AI业务层：AI圆桌发言调度引擎、实时共识分歧提炼服务
6. 前端视觉迭代：原版纯黑页面优化、天池自然风景半透磨砂背景、页脚版权信息、返回首页导航按钮改造
7. 辅助调试模块：数据库可视化看板页面（数据统计+全表可视化表格）
8. 交付收尾阶段：README部署文档、全流程Prompt记录归档、开发工作流复盘文档

## 二、AI协同开发典型问题+完整解决方案（3组真实项目踩坑案例）
1. 问题1：多discussion_id隔离失效，不同讨论房间发言、共识数据互相串流
    原因：WebSocket未按讨论ID划分独立房间、AI调度上下文未绑定单场讨论标识
    解决方案：Socket.io按discussion_id join房间；调度/共识服务全部携带讨论ID隔离上下文，单元测试增加多房间并发校验用例
2. 问题2：大模型输出自由文本，结构化共识、发言格式混乱，前端解析报错
    原因：Prompt未强约束JSON固定结构，缺少字段校验逻辑
    解决方案：调用大模型时强制返回指定JSON Schema，后端增加JSON解析容错重试，过滤无效文本
3. 问题3：原始前端全屏纯黑单调压抑，AI工具工业感过重，视觉体验差
    原因：仅使用深色纯色背景，无分层磨砂视觉、缺少柔和自然氛围感
    解决方案：全局叠加长白山天池半透明风景底图，所有面板增加磨砂玻璃半透遮罩，新增底部作者信息页脚、页面快捷导航按钮，弱化生硬纯黑风格

## 三、模块化AI协同开发设计思路
1. 拆分原则：单次提示词仅下发单一独立模块，禁止一次性生成全项目代码，降低代码耦合、便于问题定位；
2. 分层优势：先完成底层数据与接口底座，再开发实时通信、AI核心业务，最后迭代前端视觉与调试工具，底层稳定后上层业务无需反复修改；
3. 交付规范：每完成一个模块归档对应开发Prompt与开发意图，留存完整开发过程证据，满足交付文档硬性要求；
4. 测试兜底：每个后端模块配套单元测试，前端配套E2E自动化用例，保障迭代修改后功能不崩坏。
4. 文末标注项目作者信息：AI Panel Studio | 李梦冉 | 13833818010

输出要求：完整可直接写入docs/开发工作流说明.md的Markdown全文，语言专业简洁，无冗余废话。
```

```
开发意图：复盘 AI Panel Studio 完整分层 AI 协同开发流程，梳理从底层建模到最终交付全链路；记录开发过程中 3 个真实高频故障与落地解决方案，阐述单模块拆分下发提示词的工程化开发思路，满足交付文档 1~1.5 页硬性要求，作为项目完整开发过程佐证材料。
```

