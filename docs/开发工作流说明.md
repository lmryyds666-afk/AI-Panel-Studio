# 开发过程思路 & 工作流说明

> AI Panel Studio — 基于 Claude Code + DeepSeek V4 Pro 的工程化 AI 协同开发实践复盘。

---

## 一、开发流程：如何通过 Claude Code 结合 DeepSeek V4 Pro 进行开发

### 工具与模型选型

| 角色 | 选型 | 用途 |
|:--|:--|:--|
| **AI 编程助手** | Claude Code | 负责代码生成、文件编辑、Git 操作、测试执行等全流程编码任务 |
| **大语言模型** | DeepSeek V4 Pro | 负责嘉宾角色生成、发言决策调度、共识分歧提炼等 AI 业务推理 |

Claude Code 作为编程代理执行具体代码操作，DeepSeek V4 Pro 作为后端业务引擎提供 AI 推理能力——两者分工明确，互不替代。

### 分层推进范式

整个项目严格遵循 **Prompt 0 全局基准 → SDD → DDD → TDD → E2E → 视觉迭代 → 交付** 的完整流水线，每条 Prompt 只下发单一模块任务，禁止一次性生成全项目代码。14 条 Prompt 的实际执行时间线（以 `随记Prompt.md` 归档顺序为准）如下：

```
Prompt 0   项目初始化（全局基准约束：技术栈 / 分层规则 / 数据隔离 / 测试规范 / 视觉规范 / 交付归档）
    ↓
Prompt 1   SDD — SQLite ER 建模（Prisma Schema + Mermaid ER 图）
Prompt 2   SDD — REST API + WebSocket 事件契约文档
    ↓
Prompt 3   DDD — 前端演播厅页面布局与组件拆分设计
    ↓
Prompt 4   TDD — 嘉宾生成服务 + Jest 单元测试（接入 DeepSeek V4 Pro）
    ↓
Prompt 5   E2E — Cypress 端到端自动化测试脚本
    ↓
Prompt 6   TDD — 后端 REST API 路由 / 控制器 / 全局中间件
    ↓
Prompt 7   DDD — 前端 React 三页面完整实现（首页 / 配置 / 演播厅）
    ↓
Prompt 8   种子样例数据（5 组预设讨论）
    ↓
Prompt 9   TDD — Socket.io WebSocket 房间广播服务
Prompt 10  TDD — AI 圆桌发言调度引擎（DeepSeek 驱动拟人圆桌）
Prompt 11  TDD — 实时共识分歧提炼服务
    ↓
Prompt 12  DDD — 前端视觉美化（自然风景背景 + 磨砂玻璃 + 页脚导航）
    ↓
Prompt 13  交付 — 项目完整 README 部署文档
Prompt 14  交付 — 本文档（开发工作流说明）
```

> 注：Prompt 编号按归档顺序排布。实际执行中 Prompt 5（E2E 测试）在 TDD 业务层稳定后执行，符合"业务先稳再测"的原则；Prompt 6-7 在 Prompt 5 之前还是之后视联调需求灵活调整，以 `随记Prompt.md` 记录为准。

### 核心工作方式

每次交互遵循固定模式：**下发 Prompt → Claude Code 生成代码 → 本地验证运行 → Git 提交推送 → 进入下一模块**。

关键约束贯穿全程：
1. **单次单模块**：每条 Prompt 只产出当前模块的代码，不跨层溢出
2. **数据隔离强制**：所有查询/广播/内存上下文绑定 `discussionId`，杜绝多讨论串流
3. **AI 输出强约束**：调用 DeepSeek 时指定 JSON Schema 输出格式，配合 Zod 校验 + `extractJSON()` 容错清洗抑制幻觉
4. **测试先行**：每个后端模块先写 Jest 测试用例，确认失败后再实现功能
5. **交付归档**：每完成一个模块留存 Prompt + 开发意图说明，形成可追溯证据链

---

## 二、AI 协同中遇到的问题与解决路径

### 问题一：多讨论数据隔离失效，发言与共识跨房间串流

**现象**：同时运行两场讨论时，讨论 A 的发言出现在讨论 B 的 Transcript 中，共识记录也发生交叉。

**根因**：
- WebSocket 服务端未按 `discussionId` 划分独立房间，向所有客户端广播了全部事件
- `SpeechScheduler` 和 `ConsensusExtractor` 的内存上下文未绑定讨论标识，不同讨论共享同一份状态

**解决路径**：
1. Socket.IO 按 `discussion:${discussionId}` 创建命名房间，客户端连接时自动加入对应房间，广播使用 `io.to(room).emit()` 天然隔离
2. 调度器改用 `Map<discussionId, DiscussionContext>` 存储独立上下文
3. 共识提炼器维护 `Map<discussionId, ExtractionState>` 隔离状态
4. 所有数据库查询强制携带 `WHERE discussionId`
5. 单元测试增加多房间并发校验用例（同时启动 2 场讨论，验证事件隔离）

### 问题二：大模型输出自由文本，结构化解析失败导致前端白屏

**现象**：AI 返回的发言和共识夹杂 markdown 包裹、尾逗号、说明文字等非标准 JSON，前端 `JSON.parse()` 报错页面白屏。

**根因**：
- Prompt 仅描述输出格式，未强制约束 JSON Schema 字段类型
- 缺少容错清洗逻辑，直接 `JSON.parse()` 对非法输入零容忍
- 无 Zod 结构校验防线，非法字段穿透到数据库

**解决路径**：
1. Prompt 中嵌入完整 JSON Schema 模板，使用 `` ```json ``` `` 代码块引导 AI 结构化输出
2. 实现 `extractJSON()` 容错函数：优先匹配 markdown 代码块 → 括号配对扫描定位 JSON → `cleanJSON()` 移除注释与尾逗号
3. 所有 AI 输出必经 Zod `safeParse()` 校验，失败时抛出含字段路径与实际内容的详细错误
4. 调度器和提炼器各自定义独立 Zod Schema，不通用的字段不混用

### 问题三：前端演播厅发言面板空白——WebSocket 断联导致实时数据丢失

**现象**：进入演播厅页面后 Transcript 区域一直显示"等待发言..."，即使后端已正常生成发言记录并写入数据库。

**根因链路**：
- 后端研讨详情接口仅返回发言数量，未携带完整历史发言数组
- WebSocket 连接在开发模式下因 StrictMode 双挂载效应立即断开
- 前端初始化既无法从接口拿到历史数据，也无法接收实时推送，双重数据源缺失

**解决路径**：
- 修改控制器接口，返回完整 `Speech[]` 历史数组供前端初始化渲染
- 移除前端 `React.StrictMode` 避免 Socket 双挂连断开
- 在 `server.ts` 中确保 WebSocket 服务在 Express 启动后才挂载，消除竞态条件

---

## 三、对"工程化 AI 开发"的理解

### 从"一键生成"到"分层装配"

传统 AI 编程最容易陷入的误区是：给一段需求描述就让 AI 生成完整项目代码。这种方式在原型验证阶段看似高效，但一旦进入真实项目就暴露致命问题——生成的代码缺乏架构约束、模块耦合严重、修改一处牵连全身。

本项目的核心经验是：**把 AI 当作高级工程师而非代码打印机**。真正的工程师不会一口气写完整个系统，而是先建模、再定接口、再写实现、再测验收。AI 也应如此——通过逐条 Prompt 将复杂系统拆解为有依赖关系的独立模块，每步只做一件事，每步都有明确的输入契约和验证标准。

### 约束比能力更重要

AI 的代码生成能力已经很强，真正决定项目质量的不是 AI 能写出什么代码，而是**你能在多大程度上约束它的输出范围**。本项目中反复出现的模式是：

> **Prompt 里写了什么不重要，重要的是写了什么不能做。**

例如："仅输出 Prisma Schema，禁止输出接口代码、前端代码、业务逻辑"——这条否定约束比任何正面指令都有效。AI 天然倾向于给出"完整答案"，必须通过显式边界让它收敛到当前任务范围内。

### 人是架构师，AI 是执行者

工程化 AI 开发的本质关系不是替代，而是**分工**：

| 角色 | 职责 |
|:--|:--|
| **人（我）** | 架构决策、阶段划分、质量把关、边界约束、异常处理、最终验收 |
| **Claude Code** | 代码生成、文件操作、测试编写、错误修复 |
| **DeepSeek V4 Pro** | AI 业务逻辑推理（角色生成、发言决策、共识提炼） |

人负责"做什么"和"做到什么程度"，AI 负责"怎么做"。这个分工边界清晰时，协作效率最高；模糊时（比如让人去改 AI 生成的细节代码，或让 AI 自己决定架构），效率反而下降。

### 可追溯性是交付底线

作为实习交付物，代码本身只能证明"做了什么"，而 **Prompt 归档 + 工作流文档 + Git 提交历史**三件套才能证明"是怎么做的"。这既是评审要求，也是工程素养的体现——任何经得起审查的工程项目都应有完整的开发过程留痕。

---

> **AI Panel Studio** | 李梦冉 | 13833818010
